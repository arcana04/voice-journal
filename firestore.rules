rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through Cloud Functions (Admin SDK), which bypasses
    // these rules. No client (mobile app) reads or writes directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
